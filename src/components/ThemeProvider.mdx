---
name: ThemeProvider
menu: Components
---

import { Playground, Props } from 'docz';
import ThemeProvider from './ThemeProvider/ThemeProvider.tsx';
import Button from './Button/Button.tsx';

# ThemeProvider

Theming utility to style the UI. **This also inserts a global style that sets the body's**
**font to Roboto.**

> WARNING: Not yet fully implemented! Will do so soon though... that is
> once I start working on an extension plugin.

## Properties

<Props of={ThemeProvider} />

## Basic usage

<Playground>
  <ThemeProvider theme={{}}>
    <Button>Hello</Button>
  </ThemeProvider>
</Playground>
